# Backend API base URL (used when NEXT_PUBLIC_API_MOCK=false)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787

# Set to "false" to call the real backend instead of built-in mocks.
NEXT_PUBLIC_API_MOCK=true
