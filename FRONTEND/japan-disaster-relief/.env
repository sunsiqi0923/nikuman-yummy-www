# Committed defaults for all environments (local dev & CI builds).
# Override locally with .env.local (gitignored), see .env.example.

# Deployed backend Workers API
NEXT_PUBLIC_API_BASE_URL=https://api-japan-disaster-relief.tokyo-odh-108.workers.dev

# Call the real backend instead of built-in mocks
NEXT_PUBLIC_API_MOCK=false
